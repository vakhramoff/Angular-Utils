{
  "name": "@vakhramoff/angular-utils",
  "version": "0.0.6",
  "scripts": {
    "build:lib": "ng build angular-utils --prod",
    "publish:major": "npm version major && npm run build:lib && npm pack && cd ../../dist/angular-utils && npm publish",
    "publish:minor": "npm version minor && npm run build:lib && npm pack && cd ../../dist/angular-utils && npm publish",
    "publish:patch": "npm version patch && npm run build:lib && npm pack && cd ../../dist/angular-utils && npm publish"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "@angular/common": "^11.1.2",
    "@angular/core": "^11.1.2"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  }
}
